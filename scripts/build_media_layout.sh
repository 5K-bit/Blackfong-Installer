#!/usr/bin/env bash
set -euo pipefail

# Project build helper (layout only).
# A full build (ISO/IMG) will require:
# - populating an offline apt repository under assets/apt-repo/
# - generating initramfs/live root
# - EFI/U-Boot boot assets

echo "Media layout placeholder created by repo structure."
